#include <stdio.h>
#include "holberton.h"

/**
 * add - Adds 2 integers
 * @i: first operand
 * @n: second operand
 *
 * Return: an int, the result of i + ni
 */

int add(int i, int n)
{
	int res = i + n;

	return (res);
}
